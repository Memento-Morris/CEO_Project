proc sql outobs=10;
select 
    a.cust_no,
    input(strip(a.cust_no), best32.) as converted,
    b.UCN,
    b.CustRecOpenDate
from STI_PBI.APNS_DATA a
inner join WH_ENH.CUST b
    on input(strip(a.cust_no), best32.) = b.UCN
where b.period = 202605;
quit;
