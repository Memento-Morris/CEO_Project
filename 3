proc sql;
  select name, type, length
  from dictionary.columns
  where libname = 'WORK'
    and memname in ('CAMPAIGNS_TO_ADOBE_RENAMED', 'ADOBE_FINAL_TABLE_CLIENTS')
    and upcase(name) in ('CUST_NO', 'CUST_NO2');
quit;
