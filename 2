proc sql;
    select distinct delivery_code,
           count(*) as freq
    from APNS.apn_sends_and_feedback
    where product_code in (&product_list.)
    group by delivery_code
    order by freq desc;
quit;

proc contents data=APNS.apn_sends_and_feedback; run;
