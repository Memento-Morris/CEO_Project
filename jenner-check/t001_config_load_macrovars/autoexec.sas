options obs=100;
/* Bundle autoexec: cap rows, enable the same mprint/mlogic/symbolgen
   tracing the original job runs with so the macro flow is visible. */
options mprint mlogic symbolgen source notes;
